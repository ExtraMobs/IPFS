name: transpiled_ipld_prime
description: Port of go-ipld-prime.
version: 0.1.0
homepage: https://github.com/ipld/go-ipld-prime
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  transpiled_cid:
    path: ../transpiled_cid
  transpiled_multihash:
    path: ../transpiled_multihash
  transpiled_multicodec:
    path: ../transpiled_multicodec

dev_dependencies:
  test: ^1.28.0
  lints: ^6.1.0
  very_good_analysis: ^10.1.0
  analyzer: ^14.1.0 # AST parsing for tool/generate_module_index.dart
