name: boilerplate
description: Shared runtime representations of Go primitive types for audited ports.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.10.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
