---
module: cid
kind: lib/src audit
generated: 2026-08-25T08:24:06.082958
---

# Module `cid` (`lib/src/cid/`)

_Generated by `tool/generate_module_index.dart` via unresolved AST (`package:analyzer`). "Calls"/"referenced by" match by identifier name, not resolved type -- see the caveat at the top of the script. Do not edit by hand._

Depends on: [utils](utils.md)

## `lib/src/cid/cid.dart`

A Content Identifier (CID) for content-addressed data in IPFS.

_Directly tested._

### class `CID`

A Content Identifier (CID) for content-addressed data in IPFS.

- **fromContent** (method) — Creates a CID by hashing [data].
  - calls: UnsupportedError, fromList, bytes, convert, v0, sha256, v1
  - referenced by (by name):
    - `lib/src/block/block.dart` (Block.fromData)
    - `lib/src/block/block.dart` (Block.validate)
    - `lib/src/cid/cid.dart` (CID.computeForData)
    - `lib/src/cid/cid.dart` (CID.fromPrefixBytes)
- **computeForData** (method) — Computes a CID for [data] (async convenience wrapper over
  - calls: fromContent
- **computeForDataSync** (method) — Computes a CID for [data] synchronously (SHA2-256, CIDv1 only).
  - calls: fromList, bytes, convert, sha256, v1
- **fromPrefixBytes** (method) — Reconstructs a CID from a [prefix] (version + codec + multihash
  - calls: _codecFromPrefixBytes, fromContent
- **fromBytes** (method) — Parses a CID from its raw binary representation.
  - calls: isEmpty, ArgumentError, length, CID, decode, sublist, base58btc, Multibase, readVarint, FormatException, supportsByCode, name, base32
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.decode)
    - `lib/src/crypto/rsa_key.dart` (unmarshalRsaPrivateKey)
    - `lib/src/crypto/rsa_key.dart` (unmarshalRsaPublicKey)
- **decode** (method) — Decodes a CID from its string representation.
  - calls: isEmpty, ArgumentError, startsWith, decode, fromBytes
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.fromBytes)
    - `lib/src/cid/multihash.dart` (MultihashUtils.decode)
    - `lib/src/codec/dag_json_codec.dart` (DagJsonCodec.decode)
- **version** (field) — The CID version (0 or 1).
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (Prefix.==)
- **multihash** (field) — The multihash containing the hash algorithm and digest.
  - referenced by (by name):
    - `lib/src/block/block.dart` (Block.validate)
    - `lib/src/cid/cid.dart` (CID.==)
- **codec** (field) — The content codec (e.g., 'dag-pb', 'raw', 'dag-cbor').
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (Prefix.==)
- **multibaseType** (field) — The multibase encoding type for string representation.
- **encode** (method) — Encodes the CID to its string representation.
  - calls: encodeWithBase
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.validateSync)
    - `lib/src/cid/cid.dart` (CID.encodeWithBase)
    - `lib/src/cid/cid.dart` (CID.toString)
    - `lib/src/cid/multihash.dart` (MultihashUtils.sha256)
    - `lib/src/cid/multihash.dart` (MultihashUtils.encode)
    - `lib/src/cid/multihash.dart` (MultihashUtils.sum)
    - `lib/src/codec/dag_json_codec.dart` (DagJsonCodec.encode)
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.deriveKey)
    - `lib/src/crypto/rsa_key.dart` (encodePkcs1PrivateKey)
    - `lib/src/crypto/rsa_key.dart` (encodePkixPublicKey)
- **encodeWithBase** (method) — Encodes the CID using the requested [base].
  - calls: toBytes, encode, base58btc, Multibase, substring, base32
  - referenced by (by name):
    - `lib/src/cid/cid.dart` (CID.encode)
    - `lib/src/cid/cid.dart` (CID.encodeWithBaseName)
- **encodeWithBaseName** (method) — Encodes the CID using the base identified by [baseName].
  - calls: _multibaseFromName, encodeWithBase
- **toBytes** (method) — Returns the raw binary representation of the CID.
  - calls: toBytes, BytesBuilder, addByte, code, FormatException, add, encodeVarint
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.validate)
    - `lib/src/cid/cid.dart` (CID.encodeWithBase)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (CID.hashCode)
    - `lib/src/crypto/key_types.dart` (marshalKeyProto)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Component.==)
    - `lib/src/multiaddr/multiaddr.dart` (Component.hashCode)
    - `lib/src/multiaddr/multiaddr.dart` (Component.compareTo)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.toBytes)
- **toPrefixBytes** (method) — Returns the CID prefix bytes (version + codec + multihash function + hash
  - calls: toBytes, size, length, fromList, sublist
- **prefix** (method) — This CID's shape -- version, codec, hash function, and hash length --
  - calls: Prefix, name, size
- **validate** (method) — Validates the CID's structural invariants (version, codec, multihash
  - calls: size
- **toString** (method) — Returns the encoded CID string.
  - calls: encode
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.getBlock)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.putBlock)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.removeBlock)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.hasBlock)
- **==** (method)
  - calls: identical, runtimeType, version, codec, _bytesEqual, toBytes, multihash
- **hashCode** (method)
  - calls: hash, hashAll, toBytes
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.hashCode)
    - `lib/src/data_structures/immutable_bytes.dart` (ImmutableBytes.hashCode)
    - `lib/src/data_structures/typed_map.dart` (TypedMap.hashCode)

### class `Prefix`

A CID's shape -- version, codec, hash function, and hash length --

- **version** (field) — The CID version (0 or 1).
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (Prefix.==)
- **codec** (field) — The content codec (e.g. `dag-pb`, `raw`, `dag-cbor`).
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (Prefix.==)
- **mhType** (field) — The multihash function name (e.g. `sha2-256`).
  - referenced by (by name):
    - `lib/src/cid/cid.dart` (Prefix.==)
- **mhLength** (field) — The digest length in bytes.
  - referenced by (by name):
    - `lib/src/cid/cid.dart` (Prefix.==)
- **sum** (method) — Hashes [data] with [mhType] and builds a CID with this prefix's
  - calls: sum, v0, fromList, digest, v1
- **==** (method)
  - calls: identical, version, codec, mhType, mhLength
- **hashCode** (method)
  - calls: hash
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.hashCode)
    - `lib/src/data_structures/immutable_bytes.dart` (ImmutableBytes.hashCode)
    - `lib/src/data_structures/typed_map.dart` (TypedMap.hashCode)

## `lib/src/cid/multibase.dart`

Helpers for multibase encoding/decoding used by CID and other multiformats.

_Directly tested._

### class `MultibaseUtils`

Helpers for multibase encoding/decoding used by CID and other multiformats.

- **decode** (method) — Decodes a multibase-encoded string into raw bytes. The input string
  - calls: isEmpty, FormatException, first, runes, substring, length, fromCharCode, _decodeByCode
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.fromBytes)
    - `lib/src/cid/cid.dart` (CID.decode)
    - `lib/src/cid/multihash.dart` (MultihashUtils.decode)
    - `lib/src/codec/dag_json_codec.dart` (DagJsonCodec.decode)
- **encode** (method) — Encodes raw bytes using the requested [base]. Covers the 8 encodings
  - calls: base16, Multibase, base16upper, base32, base32upper, base58btc, base64, base64url, base64urlpad, _encodeByCode
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.validateSync)
    - `lib/src/cid/cid.dart` (CID.encodeWithBase)
    - `lib/src/cid/cid.dart` (CID.toString)
    - `lib/src/cid/multihash.dart` (MultihashUtils.sha256)
    - `lib/src/cid/multihash.dart` (MultihashUtils.encode)
    - `lib/src/cid/multihash.dart` (MultihashUtils.sum)
    - `lib/src/codec/dag_json_codec.dart` (DagJsonCodec.encode)
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.deriveKey)
    - `lib/src/crypto/rsa_key.dart` (encodePkcs1PrivateKey)
    - `lib/src/crypto/rsa_key.dart` (encodePkixPublicKey)
- **encodeWithName** (method) — Encodes raw bytes using the requested base name (go-multibase's
  - calls: toLowerCase, _encodeByCode

## `lib/src/cid/multicodec.dart`

Name/code lookups over the full go-multicodec registry.

_Directly tested._

### class `Multicodec`

Name/code lookups over the full go-multicodec registry.

- **code** (method) — Returns the numeric multicodec code for [name].
  - calls: ArgumentError
  - referenced by (by name) (name shared by 6 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.toBytes)
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
    - `lib/src/multiaddr/dns_resolver.dart` (dnsMatches)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.valueForProtocol)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.hasProtocol)
- **name** (method) — Returns the canonical name for [code].
  - calls: firstWhere, entries, value, ArgumentError, toRadixString, key
  - referenced by (by name) (name shared by 6 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.fromBytes)
    - `lib/src/cid/cid.dart` (CID.prefix)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toAddrString)
- **supports** (method) — Returns true if [name] is a supported multicodec.
  - calls: containsKey
- **supportsByCode** (method) — Returns true if [code] is a supported multicodec code.
  - calls: containsValue
  - referenced by (by name):
    - `lib/src/cid/cid.dart` (CID.fromBytes)
- **supported** (method) — Returns a read-only view of the supported codec names.
  - calls: unmodifiable, keys
- **count** (method) — Returns the number of registered codecs.
  - calls: length

## `lib/src/cid/multihash.dart`

Helpers for computing and decoding multihashes.

_Directly tested._

### class `MultihashUtils`

Helpers for computing and decoding multihashes.

- **sha256** (method) — Encodes [hash] with the SHA2-256 multihash function.
  - calls: length, ArgumentError, encode, Multihash
  - referenced by (by name):
    - `lib/src/cid/cid.dart` (CID.fromContent)
    - `lib/src/cid/cid.dart` (CID.computeForDataSync)
- **encode** (method) — Encodes raw [hash] bytes with the named multihash function.
  - calls: encode, Multihash
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.validateSync)
    - `lib/src/cid/cid.dart` (CID.encodeWithBase)
    - `lib/src/cid/cid.dart` (CID.toString)
    - `lib/src/cid/multihash.dart` (MultihashUtils.sha256)
    - `lib/src/cid/multihash.dart` (MultihashUtils.sum)
    - `lib/src/codec/dag_json_codec.dart` (DagJsonCodec.encode)
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.deriveKey)
    - `lib/src/crypto/rsa_key.dart` (encodePkcs1PrivateKey)
    - `lib/src/crypto/rsa_key.dart` (encodePkixPublicKey)
- **decode** (method) — Decodes a multihash byte array.
  - calls: decode, Multihash
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.fromBytes)
    - `lib/src/cid/cid.dart` (CID.decode)
    - `lib/src/codec/dag_json_codec.dart` (DagJsonCodec.decode)
- **sum** (method) — Computes the digest of [data] using the named hash function and wraps
  - calls: _digestFor, encode, Multihash
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (Prefix.sum)

