---
module: crypto
kind: lib/src audit
generated: 2026-08-25T01:44:49.454315
---

# Module `crypto` (`lib/src/crypto/`)

_Generated by `tool/generate_module_index.dart` via unresolved AST (`package:analyzer`). "Calls"/"referenced by" match by identifier name, not resolved type -- see the caveat at the top of the script. Do not edit by hand._

Depends on: —

## `lib/src/crypto/crypto_utils.dart`

Result of AES-GCM encryption containing ciphertext and nonce.

_Directly tested._

### class `EncryptedData`

Result of AES-GCM encryption containing ciphertext and nonce.

- **ciphertext** (field) — The encrypted ciphertext (includes 16-byte auth tag at end for AES-GCM).
  - referenced by (by name):
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.decrypt)
- **nonce** (field) — The 12-byte nonce/IV used for encryption.
  - referenced by (by name):
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.decrypt)
- **toBytes** (method) — Serializes to bytes: [nonce (12 bytes)] + [ciphertext].
  - calls: Uint8List, length, setAll
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.validate)
    - `lib/src/cid/cid.dart` (CID.encodeWithBase)
    - `lib/src/cid/cid.dart` (CID.toBytes)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (CID.hashCode)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Component.==)
    - `lib/src/multiaddr/multiaddr.dart` (Component.hashCode)
    - `lib/src/multiaddr/multiaddr.dart` (Component.compareTo)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.toBytes)
- **fromBytes** (method) — Deserializes from bytes.
  - calls: length, nonceSize, ArgumentError, EncryptedData, sublist
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.decode)

### class `CryptoUtils`

Cryptographic utilities for secure key management.

- **defaultIterations** (static field) — Default PBKDF2 iteration count.
- **keySize** (static field) — AES key size in bytes (256 bits).
- **nonceSize** (static field) — AES-GCM nonce size in bytes (96 bits).
  - referenced by (by name):
    - `lib/src/crypto/crypto_utils.dart` (EncryptedData.fromBytes)
- **saltSize** (static field) — PBKDF2 salt size in bytes.
- **tagSize** (static field) — AES-GCM authentication tag size in bytes (128 bits).
- **deriveKey** (method) — Derives a key from a password using PBKDF2-HMAC-SHA256 (RFC 2898).
  - calls: isEmpty, ArgumentError, length, fromList, encode, _pbkdf2HmacSha256, zeroMemory
- **encrypt** (method) — Encrypts data using AES-256-GCM.
  - calls: length, ArgumentError, randomBytes, with256bits, AesGcm, SecretKey, encrypt, Uint8List, cipherText, bytes, mac, setAll, EncryptedData
- **decrypt** (method) — Decrypts AES-256-GCM encrypted data.
  - calls: length, ArgumentError, with256bits, AesGcm, SecretKey, ciphertext, sublist, SecretBox, nonce, Mac, decrypt, fromList
- **zeroMemory** (method) — Zeros a memory buffer to prevent sensitive data from lingering.
  - calls: length
  - referenced by (by name):
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.deriveKey)
- **randomBytes** (method) — Generates cryptographically secure random bytes.
  - calls: ArgumentError, secure, fromList, generate, nextInt
  - referenced by (by name):
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.encrypt)
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.generateSalt)
- **generateSalt** (method) — Generates a random salt for PBKDF2.
  - calls: randomBytes
- **constantTimeEquals** (method) — Constant-time comparison to prevent timing attacks.
  - calls: length

## `lib/src/crypto/ed25519_signer.dart`

Unified Ed25519 signing service.

_Directly tested._

### class `Ed25519Signer`

Unified Ed25519 signing service.

- **generateKeyPair** (method) — Generates a new Ed25519 key pair.
  - calls: length, ArgumentError, newKeyPairFromSeed, newKeyPair
- **keyPairFromSeed** (method) — Creates a key pair from a 32-byte seed.
  - calls: length, ArgumentError, newKeyPairFromSeed
- **sign** (method) — Signs data using an Ed25519 private key.
  - calls: sign, fromList, bytes
- **verify** (method) — Verifies an Ed25519 signature.
  - calls: length, Signature, verify
- **extractPublicKey** (method) — Extracts the public key from a key pair.
  - calls: extractPublicKey
  - referenced by (by name):
    - `lib/src/crypto/ed25519_signer.dart` (Ed25519Signer.extractPublicKeyBytes)
- **extractPublicKeyBytes** (method) — Extracts the public key bytes from a key pair.
  - calls: extractPublicKey, fromList, bytes
- **extractSeed** (method) — Extracts the private key seed (32 bytes) from a key pair.
  - calls: extractPrivateKeyBytes, fromList, sublist
- **publicKeyFromBytes** (method) — Creates a public key from raw bytes.
  - calls: length, ArgumentError, SimplePublicKey, ed25519

