---
module: multiaddr
kind: lib/src audit
generated: 2026-08-25T01:44:49.458123
---

# Module `multiaddr` (`lib/src/multiaddr/`)

_Generated by `tool/generate_module_index.dart` via unresolved AST (`package:analyzer`). "Calls"/"referenced by" match by identifier name, not resolved type -- see the caveat at the top of the script. Do not edit by hand._

Depends on: [cid](cid.md), [utils](utils.md)

## `lib/src/multiaddr/dns_resolver.dart`

A low-level DNS resolution dependency. Equivalent to go-multiaddr-dns's

_Directly tested._

### abstract class `BasicResolver`

A low-level DNS resolution dependency. Equivalent to go-multiaddr-dns's

- **lookupIPAddr** (method) — Resolves [name] to zero or more IPv4/IPv6 address strings.
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.lookupIPAddr)
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
- **lookupTXT** (method) — Resolves the TXT records for [name].
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.lookupTXT)
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)

### class `MockResolver` implements BasicResolver

A [BasicResolver] backed by fixed maps, for tests. Equivalent to

- **ip** (field) — Domain name -> fixed IPv4/IPv6 address strings.
- **txt** (field) — Domain name -> fixed TXT record values.
- **lookupIPAddr** (method)
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.lookupIPAddr)
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
- **lookupTXT** (method)
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.lookupTXT)
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)

### class `Resolver` implements BasicResolver

Resolves `/dns4/`, `/dns6/`, `/dns/`, and `/dnsaddr/` components in a

- **withDomainResolver** (method) — Registers [resolver] as the resolver for [domain] and its
  - calls: fqdn
- **withDefaultResolver** (method) — Sets the fallback resolver used for any domain without a more
- **lookupIPAddr** (method)
  - calls: lookupIPAddr, _resolverFor
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
- **lookupTXT** (method)
  - calls: lookupTXT, _resolverFor
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
- **resolve** (method) — Resolves the first `/dns4/`, `/dns6/`, `/dns/`, or `/dnsaddr/`
  - calls: splitFunc, code, protocol, splitFirst, value, _resolverFor, lookupIPAddr, contains, add, parse, lookupTXT, _componentCount, startsWith, substring, length, _dropFirst, decapsulate, StateError, isEmpty, sublist, isNotEmpty, components, encapsulate

### top-level `isFqdn` (function)

- **isFqdn** (function) — Checks if [s] is a fully qualified domain name: ends with an unescaped
  - calls: isEmpty, endsWith, substring, length
  - referenced by (by name):
    - `lib/src/multiaddr/dns_resolver.dart` (fqdn)

### top-level `fqdn` (function)

- **fqdn** (function) — Returns the fully qualified form of [s] (a trailing dot appended, unless
  - calls: isFqdn
  - referenced by (by name):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.withDomainResolver)

### top-level `dnsMatches` (function)

- **dnsMatches** (function) — Whether [maddr] contains a `/dns4/`, `/dns6/`, `/dns/`, or `/dnsaddr/`
  - calls: forEach, code, protocol

## `lib/src/multiaddr/multiaddr.dart`

One `/protocol/value` (or bare `/protocol` for a flag protocol) segment

_Directly tested._

### class `Component`

One `/protocol/value` (or bare `/protocol` for a flag protocol) segment

- **protocol** (field) — The protocol this component's value belongs to.
  - referenced by (by name):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
    - `lib/src/multiaddr/dns_resolver.dart` (dnsMatches)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.protocols)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.valueForProtocol)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.hasProtocol)
- **valueBytes** (field) — The component's decoded value, in its raw wire-format bytes (not
- **value** (method) — The component's value in string form (empty for flag protocols).
  - calls: size, bytesToString, transcoder
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/multicodec.dart` (Multicodec.name)
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.valueForProtocol)
- **toBytes** (method) — The wire encoding of this component: varint code, an optional varint
  - calls: BytesBuilder, add, encodeVarint, code, size, isVariableSize, length, toBytes
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.validate)
    - `lib/src/cid/cid.dart` (CID.encodeWithBase)
    - `lib/src/cid/cid.dart` (CID.toBytes)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (CID.hashCode)
    - `lib/src/multiaddr/multiaddr.dart` (Component.==)
    - `lib/src/multiaddr/multiaddr.dart` (Component.hashCode)
    - `lib/src/multiaddr/multiaddr.dart` (Component.compareTo)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.toBytes)
- **toAddrString** (method) — The `/name` or `/name/value` string form of this component. A path
  - calls: size, name, isEmpty, path, startsWith
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/multiaddr/multiaddr.dart` (Component.toString)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.toAddrString)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.toString)
- **toString** (method)
  - calls: toAddrString
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.getBlock)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.putBlock)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.removeBlock)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.hasBlock)
- **==** (method)
  - calls: _bytesEqual, toBytes
- **hashCode** (method)
  - calls: hashAll, toBytes
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.hashCode)
    - `lib/src/data_structures/immutable_bytes.dart` (ImmutableBytes.hashCode)
    - `lib/src/data_structures/typed_map.dart` (TypedMap.hashCode)
- **compareTo** (method) — Lexicographic comparison of the two components' wire bytes, matching
  - calls: toBytes, length
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.compareTo)

### class `Multiaddr`

A self-describing network address: an ordered sequence of [Component]s.

- **empty** (static field) — The empty multiaddr.
  - calls: Multiaddr
  - referenced by (by name):
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.decapsulate)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.splitFunc)
- **components** (field) — This multiaddr's components, in order.
  - referenced by (by name):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.encapsulate)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.decapsulate)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.==)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.compareTo)
- **toBytes** (method) — The binary wire representation.
  - calls: BytesBuilder, add, toBytes
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.validate)
    - `lib/src/cid/cid.dart` (CID.encodeWithBase)
    - `lib/src/cid/cid.dart` (CID.toBytes)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (CID.hashCode)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Component.==)
    - `lib/src/multiaddr/multiaddr.dart` (Component.hashCode)
    - `lib/src/multiaddr/multiaddr.dart` (Component.compareTo)
- **toAddrString** (method) — The `/protocol/value/...` string representation.
  - calls: join, map, toAddrString
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/multiaddr/multiaddr.dart` (Component.toString)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.toString)
- **toString** (method)
  - calls: toAddrString
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.getBlock)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.putBlock)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.removeBlock)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.hasBlock)
- **protocols** (method) — The list of protocols present, in order.
  - calls: toList, map, protocol
- **valueForProtocol** (method) — The value of the first component matching [code], or `null` if absent
  - calls: code, protocol, value
- **hasProtocol** (method) — Whether any component uses the protocol identified by [code].
  - calls: any, code, protocol
- **encapsulate** (method) — Appends [other]'s components to this multiaddr. Equivalent to
  - calls: Multiaddr, components
  - referenced by (by name):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
- **decapsulate** (method) — Removes the last occurrence of [other]'s component sequence and
  - calls: isEmpty, components, length, empty, Multiaddr, sublist
  - referenced by (by name):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
- **forEach** (method) — Walks the components in order, stopping early if [callback] returns
  - calls: callback
  - referenced by (by name):
    - `lib/src/multiaddr/dns_resolver.dart` (dnsMatches)
- **splitFunc** (method) — Splits at the first component for which [callback] returns `true`;
  - calls: isEmpty, empty, length, callback, sublist, Multiaddr
  - referenced by (by name):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
- **splitFirst** (method) — Splits off the first component. Returns `(null, null)` if this
  - calls: isEmpty, length, Multiaddr, sublist
  - referenced by (by name):
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
- **==** (method)
  - calls: length, components
- **hashCode** (method)
  - calls: hashAll
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.hashCode)
    - `lib/src/data_structures/immutable_bytes.dart` (ImmutableBytes.hashCode)
    - `lib/src/data_structures/typed_map.dart` (TypedMap.hashCode)
- **compareTo** (method) — Component-wise lexicographic comparison, matching go-multiaddr's
  - calls: length, components, compareTo

## `lib/src/multiaddr/protocol.dart`

Marker for [Protocol.size]: the value is a varint-length-prefixed byte

_Directly tested._

### class `Transcoder`

Encodes/decodes/validates one protocol's component value. Equivalent to

- **stringToBytes** (field) — Parses a component's string-form value into its wire bytes. Throws
- **bytesToString** (field) — Renders a component's wire bytes as its string-form value.
  - referenced by (by name):
    - `lib/src/multiaddr/multiaddr.dart` (Component.value)
- **validate** (field) — Throws [FormatException] if [bytes] is not a valid encoded value.
- **validateBytes** (method) — Runs [validate] on [bytes], if set; a no-op otherwise.
  - calls: call

### class `Protocol`

A multiaddr protocol descriptor. Equivalent to go-multiaddr's `Protocol`

- **name** (field) — The protocol's canonical name, e.g. `ip4`, `tcp`, `p2p`.
  - referenced by (by name) (name shared by 6 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.fromBytes)
    - `lib/src/cid/cid.dart` (CID.prefix)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toAddrString)
- **code** (field) — The multicodec code identifying this protocol on the wire.
  - referenced by (by name) (name shared by 6 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/cid.dart` (CID.toBytes)
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
    - `lib/src/multiaddr/dns_resolver.dart` (dnsMatches)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.valueForProtocol)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.hasProtocol)
- **size** (field) — Value width in bits. `0` = no value (flag protocol). Negative
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.getStatus)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/cid.dart` (CID.prefix)
    - `lib/src/cid/cid.dart` (CID.validate)
    - `lib/src/multiaddr/multiaddr.dart` (Component.value)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toAddrString)
- **path** (field) — Whether this is a terminal "path" protocol (e.g. `unix`) that consumes
  - referenced by (by name):
    - `lib/src/multiaddr/multiaddr.dart` (Component.toAddrString)
- **transcoder** (field) — This protocol's value transcoder, or `null` for a flag protocol
  - referenced by (by name):
    - `lib/src/multiaddr/multiaddr.dart` (Component.value)
- **isVariableSize** (method) — Whether this protocol's value is varint-length-prefixed rather than a
  - referenced by (by name):
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)

### class `Protocols`

Registry of standard multiaddr protocols, mirroring go-multiaddr's

- **ip4** (static field) — IPv4 address.
- **tcp** (static field) — TCP port.
- **dns** (static field) — DNS name resolving to either an A or AAAA record.
- **dns4** (static field) — DNS name resolving to an A record.
- **dns6** (static field) — DNS name resolving to an AAAA record.
- **dnsaddr** (static field) — DNS TXT record listing further multiaddrs (bootstrap-style).
- **udp** (static field) — UDP port.
- **dccp** (static field) — DCCP port.
- **ip6** (static field) — IPv6 address.
- **ip6zone** (static field) — IPv6 zone (scope) id.
- **ipcidr** (static field) — CIDR prefix length applied to a preceding IP address.
- **quic** (static field) — QUIC (pre-v1, draft versions).
- **quicV1** (static field) — QUIC over TLS 1.3, RFC 9001.
- **webtransport** (static field) — WebTransport, layered over [quicV1].
- **certhash** (static field) — TLS/Noise certificate hash, used with [webtransport].
- **sctp** (static field) — SCTP port.
- **circuit** (static field) — Circuit Relay v2 (`/p2p-circuit`).
- **udt** (static field) — Deprecated UDP-based transport.
- **utp** (static field) — Deprecated uTorrent transport protocol.
- **unix** (static field) — Unix domain socket path.
- **p2p** (static field) — libp2p PeerId.
- **ipfs** (static field) — Alias for [p2p], kept for backwards compatibility (same code).
- **http** (static field) — HTTP.
- **httpPath** (static field) — A URL path, for use after [http]/[https].
- **https** (static field) — Deprecated alias for `/tls/http`.
- **onion** (static field) — Tor v2 onion address (deprecated by the Tor network itself).
- **onion3** (static field) — Tor v3 onion address.
- **garlic64** (static field) — I2P destination, base64-encoded.
- **garlic32** (static field) — I2P destination, base32-encoded.
- **p2pWebRtcDirect** (static field) — Deprecated; use [webrtcDirect] instead.
- **tls** (static field) — TLS.
- **sni** (static field) — TLS/QUIC Server Name Indication hostname.
- **noise** (static field) — Noise transport security handshake.
- **ws** (static field) — WebSocket.
- **wss** (static field) — Deprecated alias for `/tls/ws`.
- **plaintextv2** (static field) — Deprecated plaintext transport security (multistream v2 negotiation).
- **webrtcDirect** (static field) — WebRTC with an out-of-band signaling channel.
- **webrtc** (static field) — Browser-to-browser WebRTC.
- **memory** (static field) — In-process transport, addressed by an opaque 64-bit id.
- **all** (static field) — All registered protocols, in go-multiaddr's `init()` registration
  - calls: Protocol
- **byName** (method) — Looks up a protocol by its string name (e.g. `ip4`), or `null` if
- **byCode** (method) — Looks up a protocol by its multicodec code, or `null` if unknown.

### top-level `kLengthPrefixedVarSize` (variable)

- **kLengthPrefixedVarSize** (variable) — Marker for [Protocol.size]: the value is a varint-length-prefixed byte

## `lib/src/multiaddr/transcoders.dart`

Parses a textual IPv6 address (RFC 4291, including `::` compression and

_No known direct test._

### top-level `transcoderIP4` (variable)

- **transcoderIP4** (variable) — Transcoder for `ip4` (4-byte dotted-decimal).
  - calls: Transcoder

### top-level `transcoderIP6` (variable)

- **transcoderIP6** (variable) — Transcoder for `ip6` (16-byte RFC 5952 canonical text).
  - calls: Transcoder

### top-level `transcoderIP6Zone` (variable)

- **transcoderIP6Zone** (variable) — Transcoder for `ip6zone` (UTF-8 zone id, no `/`).
  - calls: Transcoder

### top-level `transcoderIPCIDR` (variable)

- **transcoderIPCIDR** (variable) — Transcoder for `ipcidr` (1-byte prefix length, 0-255).
  - calls: Transcoder

### top-level `transcoderPort` (variable)

- **transcoderPort** (variable) — Transcoder for `tcp`/`udp`/`dccp`/`sctp` (2-byte big-endian port).
  - calls: Transcoder

### top-level `transcoderDns` (variable)

- **transcoderDns** (variable) — Transcoder for `dns`/`dns4`/`dns6`/`dnsaddr`/`sni` (UTF-8 hostname, no
  - calls: Transcoder

### top-level `transcoderOnion` (variable)

- **transcoderOnion** (variable) — Transcoder for `onion` (Tor v2 address, 10-byte host + 2-byte port).
  - calls: Transcoder

### top-level `transcoderOnion3` (variable)

- **transcoderOnion3** (variable) — Transcoder for `onion3` (Tor v3 address, 35-byte host + 2-byte port).
  - calls: Transcoder

### top-level `transcoderGarlic64` (variable)

- **transcoderGarlic64** (variable) — Transcoder for `garlic64` (I2P base64 destination, i2p alphabet).
  - calls: Transcoder

### top-level `transcoderGarlic32` (variable)

- **transcoderGarlic32** (variable) — Transcoder for `garlic32` (I2P base32 destination, unpadded).
  - calls: Transcoder

### top-level `transcoderP2P` (variable)

- **transcoderP2P** (variable) — Transcoder for `p2p` (a PeerId: base58 multihash, or a `libp2p-key` CID).
  - calls: Transcoder

### top-level `transcoderUnix` (variable)

- **transcoderUnix** (variable) — Transcoder for `unix` (UTF-8 filesystem path, must start with `/`).
  - calls: Transcoder

### top-level `transcoderCertHash` (variable)

- **transcoderCertHash** (variable) — Transcoder for `certhash` (a multibase-encoded multihash).
  - calls: Transcoder

### top-level `transcoderHTTPPath` (variable)

- **transcoderHTTPPath** (variable) — Transcoder for `http-path` (a query-escaped URL path).
  - calls: Transcoder

### top-level `transcoderMemory` (variable)

- **transcoderMemory** (variable) — Transcoder for `memory` (8-byte big-endian unsigned integer).
  - calls: Transcoder

