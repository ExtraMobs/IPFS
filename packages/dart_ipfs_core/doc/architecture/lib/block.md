---
module: block
kind: lib/src audit
generated: 2026-08-25T08:24:06.074382
---

# Module `block` (`lib/src/block/`)

_Generated by `tool/generate_module_index.dart` via unresolved AST (`package:analyzer`). "Calls"/"referenced by" match by identifier name, not resolved type -- see the caveat at the top of the script. Do not edit by hand._

Depends on: [cid](cid.md)

## `lib/src/block/block.dart`

Interface for content-addressed data blocks.

_Directly tested._

### abstract class `IBlock`

Interface for content-addressed data blocks.

- **data** (method) — The raw binary data of this block.
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.==)
- **cid** (method) — The content identifier for this block.
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.==)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.putBlock)
- **size** (method) — The size of this block in bytes.
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.getStatus)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/cid.dart` (CID.prefix)
    - `lib/src/cid/cid.dart` (CID.validate)
    - `lib/src/multiaddr/multiaddr.dart` (Component.value)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toAddrString)
- **toBytes** (method) — Serializes the block to bytes.
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.validate)
    - `lib/src/cid/cid.dart` (CID.encodeWithBase)
    - `lib/src/cid/cid.dart` (CID.toBytes)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (CID.hashCode)
    - `lib/src/crypto/key_types.dart` (marshalKeyProto)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Component.==)
    - `lib/src/multiaddr/multiaddr.dart` (Component.hashCode)
    - `lib/src/multiaddr/multiaddr.dart` (Component.compareTo)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.toBytes)
- **validate** (method) — Validates the block's content hash matches its CID.

### class `Block` implements IBlock

Represents an IPFS block.

- **fromData** (method) — Creates a block from raw data, automatically computing the CID.
  - calls: fromContent, Block
- **cid** (field)
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.==)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.putBlock)
- **data** (field)
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.==)
- **format** (field) — The codec format used by this block (e.g., 'raw', 'dag-pb').
- **size** (method)
  - calls: length
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.getStatus)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/cid.dart` (CID.prefix)
    - `lib/src/cid/cid.dart` (CID.validate)
    - `lib/src/multiaddr/multiaddr.dart` (Component.value)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toAddrString)
- **validate** (method)
  - calls: fromContent, toBytes, multihash, length
- **validateSync** (method) — Synchronous structural validation.
  - calls: isEmpty, encode
- **toBytes** (method)
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.validate)
    - `lib/src/cid/cid.dart` (CID.encodeWithBase)
    - `lib/src/cid/cid.dart` (CID.toBytes)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/cid.dart` (CID.==)
    - `lib/src/cid/cid.dart` (CID.hashCode)
    - `lib/src/crypto/key_types.dart` (marshalKeyProto)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Component.==)
    - `lib/src/multiaddr/multiaddr.dart` (Component.hashCode)
    - `lib/src/multiaddr/multiaddr.dart` (Component.compareTo)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.toBytes)
- **==** (method)
  - calls: identical, runtimeType, cid, _bytesEqual, data
- **hashCode** (method)
  - calls: hashCode, length
  - referenced by (by name) (name shared by 7 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/data_structures/immutable_bytes.dart` (ImmutableBytes.hashCode)
    - `lib/src/data_structures/typed_map.dart` (TypedMap.hashCode)

## `lib/src/block/block_store.dart`

Point-in-time statistics for a block store.

_No known direct test._

### class `BlockStoreStatus`

Point-in-time statistics for a block store.

- **blockCount** (field) — Number of blocks currently held.
- **totalSize** (field) — Combined size in bytes of all held blocks.

### class `BlockStoreResult`

A generic result returned by block store operations.

- **succeeded** (field) — Whether the operation succeeded.
- **value** (field) — The operation payload.
  - referenced by (by name) (name shared by 2 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/cid/multicodec.dart` (Multicodec.name)
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.valueForProtocol)
- **message** (field) — Optional human-readable message.

### abstract class `IBlockStore` extends ILifecycle

Interface for block storage operations.

- **getBlock** (method) — Retrieves a block by its CID.
- **putBlock** (method) — Stores a block.
- **removeBlock** (method) — Removes a block by its CID.
- **hasBlock** (method) — Returns true if the block exists.
- **getAllBlocks** (method) — Returns all stored blocks.
- **getStatus** (method) — Returns a point-in-time snapshot of block count and total size.
- **gc** (method) — Removes unreachable blocks and returns the number removed.

## `lib/src/block/memory_block_store.dart`

A simple in-memory block store implementation.

_Directly tested._

### class `InMemoryBlockStore` implements IBlockStore

A simple in-memory block store implementation.

- **start** (method)
- **stop** (method)
  - calls: clear
- **getBlock** (method)
  - calls: toString, BlockStoreResult
- **putBlock** (method)
  - calls: toString, cid, BlockStoreResult
- **removeBlock** (method)
  - calls: toString, remove, BlockStoreResult
- **hasBlock** (method)
  - calls: containsKey, toString
- **getAllBlocks** (method)
  - calls: unmodifiable, values
- **getStatus** (method)
  - calls: values, size, BlockStoreStatus, length
- **gc** (method)
- **length** (method) — Returns the number of blocks currently in memory.
  - calls: length
  - referenced by (by name) (name shared by 3 declarations -- not resolved to this one specifically, see caveat):
    - `lib/src/block/block.dart` (Block.size)
    - `lib/src/block/block.dart` (Block.validate)
    - `lib/src/block/block.dart` (Block.hashCode)
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.getStatus)
    - `lib/src/cid/cid.dart` (CID.fromBytes)
    - `lib/src/cid/cid.dart` (CID.toPrefixBytes)
    - `lib/src/cid/multibase.dart` (MultibaseUtils.decode)
    - `lib/src/cid/multicodec.dart` (Multicodec.count)
    - `lib/src/cid/multihash.dart` (MultihashUtils.sha256)
    - `lib/src/crypto/crypto_utils.dart` (EncryptedData.toBytes)
    - `lib/src/crypto/crypto_utils.dart` (EncryptedData.fromBytes)
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.deriveKey)
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.encrypt)
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.decrypt)
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.zeroMemory)
    - `lib/src/crypto/crypto_utils.dart` (CryptoUtils.constantTimeEquals)
    - `lib/src/crypto/ed25519_signer.dart` (Ed25519Signer.generateKeyPair)
    - `lib/src/crypto/ed25519_signer.dart` (Ed25519Signer.keyPairFromSeed)
    - `lib/src/crypto/ed25519_signer.dart` (Ed25519Signer.verify)
    - `lib/src/crypto/ed25519_signer.dart` (Ed25519Signer.publicKeyFromBytes)
    - `lib/src/crypto/key_types.dart` (marshalKeyProto)
    - `lib/src/crypto/key_types.dart` (unmarshalKeyProto)
    - `lib/src/crypto/rsa_key.dart` (unmarshalRsaPrivateKey)
    - `lib/src/crypto/rsa_key.dart` (unmarshalRsaPublicKey)
    - `lib/src/data_structures/immutable_bytes.dart` (ImmutableBytes.length)
    - `lib/src/data_structures/typed_map.dart` (TypedMap.length)
    - `lib/src/multiaddr/dns_resolver.dart` (Resolver.resolve)
    - `lib/src/multiaddr/dns_resolver.dart` (isFqdn)
    - `lib/src/multiaddr/multiaddr.dart` (Component.toBytes)
    - `lib/src/multiaddr/multiaddr.dart` (Component.compareTo)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.decapsulate)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.splitFunc)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.splitFirst)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.==)
    - `lib/src/multiaddr/multiaddr.dart` (Multiaddr.compareTo)
    - `lib/src/utils/varint.dart` (readVarint)
- **clear** (method) — Clears all blocks from the store.
  - calls: clear
  - referenced by (by name):
    - `lib/src/block/memory_block_store.dart` (InMemoryBlockStore.stop)

