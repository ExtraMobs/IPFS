name: transpiled_base58
description: Port of github.com/mr-tron/base58 (Bitcoin/IPFS-alphabet base58 encoding, used for legacy libp2p PeerId string representation).
version: 0.1.0
homepage: https://github.com/mr-tron/base58

environment:
  sdk: ^3.10.0

dev_dependencies:
  test: ^1.28.0
  lints: ^6.1.0
  very_good_analysis: ^10.1.0
  analyzer: ^14.1.0 # AST parsing for tool/generate_module_index.dart
